services:
  app:
    image: wisdomwork/ecode-admin:latest
    platform: linux/amd64
    ports:
      - '4001:80'
    restart: always
    env_file:
      - .env
    environment:
      - VITE_API_URL=${VITE_API_URL}
